<div class="hero-blob-field absolute inset-0 overflow-hidden" aria-hidden="true">
  <div class="hero-blob hero-blob--one"></div>
  <div class="hero-blob hero-blob--two"></div>
  <div class="hero-blob hero-blob--three"></div>
  <div class="hero-contours absolute inset-[-8%]"></div>
</div>

<style>
  .hero-blob-field {
    background:
      radial-gradient(
        ellipse at 76% 58%,
        var(--hero-field-1) 0%,
        transparent 48%
      ),
      linear-gradient(145deg, var(--hero-field-0) 0%, var(--hero-field-2) 100%);
    isolation: isolate;
  }

  .hero-blob {
    position: absolute;
    border-radius: 45% 55% 58% 42% / 54% 42% 58% 46%;
    filter: blur(5.5rem);
    will-change: transform;
  }

  .hero-blob--one {
    top: -18%;
    left: -14%;
    width: 76%;
    height: 68%;
    background: var(--hero-field-2);
    opacity: 0.7;
    animation: blob-breathe-one 16s ease-in-out infinite alternate;
  }

  .hero-blob--two {
    right: -19%;
    bottom: -22%;
    width: 82%;
    height: 76%;
    background: var(--hero-field-1);
    opacity: 0.54;
    animation: blob-breathe-two 19s ease-in-out infinite alternate;
  }

  .hero-blob--three {
    top: 31%;
    left: 24%;
    width: 46%;
    height: 42%;
    background: var(--hero-field-0);
    opacity: 0.78;
    animation: blob-breathe-three 13s ease-in-out infinite alternate;
  }

  .hero-contours {
    background: repeating-radial-gradient(
      ellipse at 68% 54%,
      transparent 0 2.15rem,
      color-mix(in oklab, var(--accent-contrast) 2%, transparent) 2.22rem 2.28rem
    );
    mix-blend-mode: soft-light;
    opacity: 0.22;
    transform-origin: 68% 54%;
    animation: contour-breathe 22s ease-in-out infinite alternate;
  }

  @keyframes blob-breathe-one {
    from {
      transform: translate3d(-2%, -1%, 0) scale(0.96) rotate(-4deg);
    }
    to {
      transform: translate3d(7%, 6%, 0) scale(1.08) rotate(5deg);
    }
  }

  @keyframes blob-breathe-two {
    from {
      transform: translate3d(3%, 4%, 0) scale(1.02) rotate(2deg);
    }
    to {
      transform: translate3d(-7%, -5%, 0) scale(0.93) rotate(-5deg);
    }
  }

  @keyframes blob-breathe-three {
    from {
      transform: translate3d(-4%, 3%, 0) scale(0.94);
    }
    to {
      transform: translate3d(7%, -5%, 0) scale(1.1);
    }
  }

  @keyframes contour-breathe {
    from {
      transform: translate3d(-1.5%, 1%, 0) scale(0.98);
    }
    to {
      transform: translate3d(1.5%, -1%, 0) scale(1.035);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-blob,
    .hero-contours {
      animation: none;
    }
  }
</style>
