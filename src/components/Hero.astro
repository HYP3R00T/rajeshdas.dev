---
// Terminal Hero Section Component
// Displays the main hero section with terminal command and feature grid
import { Icon } from "astro-icon/components";

const attributes = [
  {
    title: "Automation First",
    icon: "zap",
    description:
      "If it can be automated, it should be. Building tools that eliminate repetitive tasks.",
  },
  {
    title: "Open Source",
    icon: "code",
    description:
      "Contributing to the community and sharing knowledge through open-source projects.",
  },
  {
    title: "Cloud Native",
    icon: "git-branch",
    description:
      "Designing scalable, resilient infrastructure for modern distributed systems.",
  },
];
---

<section
  class="stripe-pattern-dense relative flex items-center justify-center px-4 py-16 md:px-12"
>
  <div class="mx-auto w-full max-w-7xl">
    <div class="mb-20 space-y-8">
      <!-- Badge -->
      <div
        class="inline-block border border-white bg-white px-3 py-1.5 font-mono text-xs font-medium tracking-wider text-black"
      >
        DEVOPS ENGINEER
      </div>

      <!-- Main Grid Layout -->
      <div class="grid grid-cols-1 gap-12 lg:grid-cols-2 lg:gap-20">
        <!-- Left Column - Main Content -->
        <div>
          <!-- Main Heading -->
          <h1
            class="mb-8 text-left font-mono text-[clamp(3rem,10vw,7rem)] leading-[0.9] tracking-tighter text-white uppercase"
          >
            <div>RAJESH</div>
            <div>DAS</div>
          </h1>

          <!-- Subtitle -->
          <div class="max-w-xl space-y-6">
            <p
              class="text-foreground-muted text-base leading-relaxed md:text-lg"
            >
              Building robust infrastructure and automation tools that empower
              development teams. Specialized in cloud architecture, CI/CD
              pipelines, and developer experience.
            </p>
            <p class="text-foreground-1 text-sm leading-relaxed">
              From infrastructure as code to container orchestration, I focus on
              creating reliable, scalable systems that let teams ship faster and
              sleep better.
            </p>
          </div>
        </div>

        <!-- Right Column - Attributes -->
        <div class="space-y-4">
          {
            attributes.map((attribute) => (
              <div class="border-border-strong hover:bg-background-hover border bg-black p-6 transition-colors">
                <div class="flex gap-4">
                  <div class="border-border-stronger flex h-12 w-12 shrink-0 items-center justify-center border">
                    <Icon name={attribute.icon} class="h-5 w-5 text-white" />
                  </div>
                  <div>
                    <h3 class="font-display mb-2 text-base tracking-wide text-white uppercase">
                      {attribute.title}
                    </h3>
                    <p class="text-foreground-1 text-sm leading-relaxed">
                      {attribute.description}
                    </p>
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>

    <!-- CLI Command Box -->
    <!-- <div class="mt-20 mb-20">
      <div class="text-section-label mb-3">Try the CLI</div>
      <div
        class="group border-border-strong relative max-w-3xl border bg-black"
      >
        <div class="flex items-center justify-between p-6">
          <code class="font-mono text-lg tracking-tight text-white md:text-2xl">
            $ uvx rajesh-das
          </code>
          <button
            id="copy-button"
            class="border-border-strong border p-3 opacity-0 transition-colors group-hover:opacity-100 hover:bg-white hover:text-black"
            aria-label="Copy command"
            title="Copy to clipboard"
          >
            <Icon id="copy-icon" name="copy" class="h-5 w-5" />
            <Icon id="check-icon" name="plus" class="hidden h-5 w-5" />
          </button>
        </div>
      </div>
      <p class="text-foreground-2 mt-4 max-w-3xl text-xs">
        Experience my portfolio directly in your terminal. Run the command above
        using uvx to explore my work, skills, and experience in an interactive
        CLI.
      </p>
    </div> -->

    <!-- Feature Grid -->
    <!-- <div
      class="border-border grid max-w-5xl grid-cols-1 gap-px border md:grid-cols-3"
    >
      <div class="border-border border-r bg-black p-8 last:border-r-0">
        <div class="text-section-label mb-3">CLOUD EXPERTISE</div>
        <div class="text-sm text-white">AWS • Azure • GCP</div>
      </div>
      <div class="border-border border-r bg-black p-8 last:border-r-0">
        <div class="text-section-label mb-3">CONTAINER ORCHESTRATION</div>
        <div class="text-sm text-white">Kubernetes • Docker</div>
      </div>
      <div class="border-border border-r bg-black p-8 last:border-r-0">
        <div class="text-section-label mb-3">INFRASTRUCTURE AS CODE</div>
        <div class="text-sm text-white">Terraform • Ansible</div>
      </div>
    </div> -->

    <!-- Scroll Indicator -->
    <div class="absolute bottom-12 left-1/2 -translate-x-1/2">
      <div
        class="text-foreground-3 flex animate-bounce flex-col items-center gap-2"
      >
        <Icon name="chevron-down-double" class="size-6" />
      </div>
    </div>
  </div>
</section>

<!-- <script>
  // Copy to clipboard functionality
  const copyButton = document.getElementById("copy-button");
  const copyIcon = document.getElementById("copy-icon");
  const checkIcon = document.getElementById("check-icon");
  const command = "uvx rajesh-das";

  if (copyButton && copyIcon && checkIcon) {
    copyButton.addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText(command);
        // Show check icon
        copyIcon.classList.add("hidden");
        checkIcon.classList.remove("hidden");
        // Reset after 2 seconds
        setTimeout(() => {
          copyIcon.classList.remove("hidden");
          checkIcon.classList.add("hidden");
        }, 2000);
      } catch (err) {
        console.error("Failed to copy:", err);
      }
    });
  }
</script> -->
